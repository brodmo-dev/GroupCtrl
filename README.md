# GroupCtrl

## Usage

Target: The selected app will always open first, even if it isn't currently running.

## Development

- Requires `npm`
- `cargo run`

### Hot reload (macOS only)

- One-time: `cargo install dioxus-cli`
- `dx serve` & `npm run watch`
