[application]
name = "GroupCtrl"
default_platform = "desktop"

[bundle]
identifier = "dev.brodmo.groupctrl"
publisher = "Moritz Brödel"
icon = ["assets/icons/icon.icns"]

# TODO this doesn't end up in the right bundle
[macos.plist]
LSUIElement = true
